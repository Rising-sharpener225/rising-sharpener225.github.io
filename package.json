{
  "name": "plugins-compiler",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc --skipLibCheck true && node build.js"
  },
  "dependencies": {
    "cheerio": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
